

<div class="nameu">
  <%= @user.username %>
</div>
<p>Admin <%= @user.isadmin %></p>

<% if user_signed_in? && current_user.isadmin == true %>
  <p>Admin <%= @user.isadmin %></p>
<% end %>


Закрытые группы <%= @user.favorites.count %> <%= (@user.favorites.count) == 1 ? 'Post' : 'Posts'%>
<% @posts.each do |post| %>
  <br><%= link_to post.title, post %>
<% end %>

Сохраненное <%= @user.favorites.count %> <%= (@user.favorites.count) == 1 ? 'Post' : 'Posts'%>
<% @posts.each do |post| %>
  <br><%= link_to post.title, post %>
<% end %>


<% if user_signed_in? && @user.id == current_user.id %>

    <%= link_to 'редактировать', :class => "editp",  edit_user_registration_path %>

<% end %>
